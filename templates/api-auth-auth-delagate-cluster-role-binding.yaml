{{- if and .Values.kubeAPI.operator.clusterRole.enabled .Values.store.openbao.proxy.authMethod.openBaoUsesSelfJwtReviewer}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: "{{ .Values.kubeAPI.apiAuth.clusterRole.name }}-auth-delegator"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: "system:auth-delegator"
subjects:
  - kind: ServiceAccount
    name: {{ .Values.kubeAPI.apiAuth.serviceAccount.name }}
    namespace: {{ .Release.Namespace }}
{{- end }}