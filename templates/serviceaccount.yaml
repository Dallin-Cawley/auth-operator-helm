{{- if .Values.kubeAPI.operator.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.kubeAPI.operator.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "auth-operator.labels" . | nindent 4 }}
  {{- with .Values.kubeAPI.operator.serviceAccount.annotations }}
  annotations:
    {{ toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: true
{{- end }}