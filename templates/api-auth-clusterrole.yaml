{{- if .Values.kubeAPI.operator.clusterRole.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: {{ include "api-auth.name" . }}
  name: {{ .Values.kubeAPI.apiAuth.clusterRole.name }}
rules:
{{- end }}
